// Generated by LiveScript 1.6.0
(function(){
  var gunHUD, HUD;
  gunHUD = (function(superclass){
    var prototype = extend$((import$(gunHUD, superclass).displayName = 'gunHUD', gunHUD), superclass).prototype, constructor = gunHUD;
    function gunHUD(gl, pos){
      gunHUD.superclass.call(this, gl, pos);
      this.texture = Resources.getTexture(gl, "doomPistol.png");
      this.anim.xTiles = 3;
      this.anim.yTiles = 2;
    }
    gunHUD.prototype.render = function(projectionMatrix){
      var viewMatrix;
      viewMatrix = mat4.create();
      mat4.identity(viewMatrix);
      console.log("Era pra desenhar alguma coisa");
      return superclass.prototype.render.call(this, viewMatrix, projectionMatrix);
    };
    return gunHUD;
  }(Sprite));
  window.HUD = HUD = (function(){
    HUD.displayName = 'HUD';
    var prototype = HUD.prototype, constructor = HUD;
    function HUD(gl){
      this.gl = gl;
      this.gun = new gunHUD(gl, [1.2, -2.1, -0.1]);
      this.text = new Text("Puta dor nas costas socorro", "Lucidas Console", [100, 300], 32, "#ffffff", true);
    }
    HUD.prototype.render = function(){
      var orthoProjectionMatrix;
      orthoProjectionMatrix = mat4.create();
      mat4.ortho(orthoProjectionMatrix, -2.0, 2.0, -2.0, 2.0, 0.1, 5.0);
      return this.gun.render(orthoProjectionMatrix);
    };
    return HUD;
  }());
  function extend$(sub, sup){
    function fun(){} fun.prototype = (sub.superclass = sup).prototype;
    (sub.prototype = new fun).constructor = sub;
    if (typeof sup.extended == 'function') sup.extended(sub);
    return sub;
  }
  function import$(obj, src){
    var own = {}.hasOwnProperty;
    for (var key in src) if (own.call(src, key)) obj[key] = src[key];
    return obj;
  }
}).call(this);
